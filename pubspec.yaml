name: flutter_hw_info_app
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.17.6 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  sensors_plus: ^1.3.4+1
  cpu_reader: ^3.0.0
  system_info_plus: ^0.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  lint: ^1.10.0

flutter:
  uses-material-design: true
