import 'package:flutter/material.dart';

class SoCInfoPage extends StatelessWidget {
  const SoCInfoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('SoC'));
  }
}
