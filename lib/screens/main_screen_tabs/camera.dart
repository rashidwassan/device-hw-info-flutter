import 'package:flutter/material.dart';

class CameraInfoPage extends StatelessWidget {
  const CameraInfoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Camera Info Page'));
  }
}
