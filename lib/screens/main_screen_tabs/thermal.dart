import 'package:flutter/material.dart';

class ThermalInfoPage extends StatelessWidget {
  const ThermalInfoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Thermal Info'));
  }
}
