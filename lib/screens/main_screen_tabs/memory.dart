import 'package:flutter/material.dart';

class MemoryInfoPage extends StatelessWidget {
  const MemoryInfoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Container());
  }
}
